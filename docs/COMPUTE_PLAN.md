# Compute plan

This pipeline is designed to run on a typical laptop.

Typical first run costs (approximate):
- Downloads: a few hundred MB (depends on GEO supplementary files).
- Runtime: ~10–30 minutes depending on network speed and CPU.

The pipeline caches downloads under `data/` for subsequent runs.
